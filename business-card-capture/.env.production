# Production — connects to live Power Platform connectors
VITE_USE_MOCK_CONNECTORS=false

# Salesforce dual-write — set to true once the shared_salesforce connection is
# wired up via: pac code add-data-source -a shared_salesforce -c <connectionId>
VITE_SYNC_TO_SALESFORCE=false
