# Development — uses mock connectors (no Power Platform needed)
VITE_USE_MOCK_CONNECTORS=true

# Salesforce dual-write (off by default in dev — mock path is used when false)
VITE_SYNC_TO_SALESFORCE=false
